//there are already many maths functions that are inbuilt in js
//here lets try them out


let variable=Math.PI;
variable=Math.LN2;
variable=Math.LN10; 
variable=Math.random();
variable=Math.ceil(variable);
variable=Math.abs(variable);
variable=Math.sqrt(variable);
variable=Math.pow(variable,-2);
variable=Math.floor(variable);
variable=Math.round(variable);
variable=Math.min(0,2,3,4,5,8);
variable=Math.max(5,7,3,2,1);
//time is also an inbuilt object
variable=new Date();
